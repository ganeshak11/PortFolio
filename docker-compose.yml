version: "3.9"

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: portfolio:local
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
